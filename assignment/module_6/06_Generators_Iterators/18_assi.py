# List of integers
numbers = [10, 20, 30, 40, 50]

# Initialize the index
index = 0

# Iterate manually like a custom iterator
print("Iterating over the list using a manual iterator:")
while index < len(numbers):
    print(numbers[index])
    index += 1